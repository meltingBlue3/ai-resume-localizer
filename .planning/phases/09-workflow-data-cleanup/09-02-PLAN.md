---
phase: 09-workflow-data-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - frontend/src/components/review/ResumeFieldEditor.tsx
  - frontend/src/components/review/JpResumeFieldEditor.tsx
  - frontend/src/utils/completeness.ts
  - frontend/src/i18n/locales/zh/wizard.json
  - frontend/src/i18n/locales/ja/wizard.json
autonomous: true
requirements: [EXTR-01, EXTR-02, TRAN-01]

must_haves:
  truths:
    - "Chinese resume editor shows `other` field label instead of `hobbies`"
    - "Japanese resume editor no longer shows linkedin, website, gpa, or notes input fields"
    - "Completeness calculation references `other` instead of `hobbies`"
    - "i18n labels exist for the `other` field in both zh and ja locales"
  artifacts:
    - path: "frontend/src/components/review/ResumeFieldEditor.tsx"
      provides: "Chinese resume editor with `other` field instead of `hobbies`"
    - path: "frontend/src/components/review/JpResumeFieldEditor.tsx"
      provides: "Japanese resume editor without linkedin/website/gpa/notes fields"
    - path: "frontend/src/utils/completeness.ts"
      provides: "Completeness utility referencing `other` instead of `hobbies`"
    - path: "frontend/src/i18n/locales/zh/wizard.json"
      provides: "Chinese i18n labels for `other` field"
      contains: "other"
    - path: "frontend/src/i18n/locales/ja/wizard.json"
      provides: "Japanese i18n labels for `other` field"
      contains: "other"
  key_links:
    - from: "frontend/src/components/review/ResumeFieldEditor.tsx"
      to: "frontend/src/types/resume.ts"
      via: "CnResumeData.other field access"
      pattern: "\\.other"
    - from: "frontend/src/components/review/JpResumeFieldEditor.tsx"
      to: "frontend/src/types/resume.ts"
      via: "JpPersonalInfo and JpEducationEntry field access (linkedin/website/gpa/notes removed)"
    - from: "frontend/src/utils/completeness.ts"
      to: "frontend/src/types/resume.ts"
      via: "Field name references for scoring"
      pattern: "other"
---

<objective>
Update frontend UI components, i18n labels, and completeness utility to reflect the data model changes from Plan 01 (hobbies→other rename, removed Jp fields).

Purpose: Ensure the user-facing editor forms and completeness scoring match the updated data contracts.
Output: Clean frontend with no references to removed fields, proper labels for the new `other` field.
</objective>

<execution_context>
@C:/Users/zhang/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zhang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-workflow-data-cleanup/09-01-SUMMARY.md
@frontend/src/types/resume.ts
@frontend/src/components/review/ResumeFieldEditor.tsx
@frontend/src/components/review/JpResumeFieldEditor.tsx
@frontend/src/utils/completeness.ts
@frontend/src/i18n/locales/zh/wizard.json
@frontend/src/i18n/locales/ja/wizard.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update frontend editors — rename hobbies→other, remove dead Jp fields</name>
  <files>frontend/src/components/review/ResumeFieldEditor.tsx, frontend/src/components/review/JpResumeFieldEditor.tsx</files>
  <action>
**ResumeFieldEditor.tsx (Chinese resume editor):**

1. Find all references to `hobbies` and rename to `other`:
   - Field access: `data.hobbies` → `data.other`
   - onChange handler: update field name from `hobbies` to `other`
   - i18n label key: update from `hobbies` to `other` (e.g., `t('reviewExtraction.fields.hobbies')` → `t('reviewExtraction.fields.other')`)
   - The `other` field should remain a textarea input (same as hobbies was), since it contains free-form text

**JpResumeFieldEditor.tsx (Japanese resume editor):**

2. Remove `linkedin` and `website` fields from the personal_info section:
   - Find the input fields for `linkedin` and `website` and delete them entirely (JSX elements + any onChange handlers)
   - These fields no longer exist in JpPersonalInfo interface

3. Remove `gpa` and `notes` fields from the education section:
   - Find the input fields for `gpa` and `notes` within education entry rendering and delete them
   - These fields no longer exist in JpEducationEntry interface

Verify there are no TypeScript errors after removal by checking that no code references the deleted field names.
  </action>
  <verify>
- Read ResumeFieldEditor.tsx and confirm no `hobbies` references remain, `other` is used instead
- Read JpResumeFieldEditor.tsx and confirm no `linkedin`, `website`, `gpa`, `notes` references remain
- Run: `cd frontend && npx tsc --noEmit` to verify no TypeScript errors
  </verify>
  <done>ResumeFieldEditor uses `other` instead of `hobbies`. JpResumeFieldEditor no longer renders linkedin, website, gpa, or notes fields.</done>
</task>

<task type="auto">
  <name>Task 2: Update i18n labels and completeness utility</name>
  <files>frontend/src/i18n/locales/zh/wizard.json, frontend/src/i18n/locales/ja/wizard.json, frontend/src/utils/completeness.ts</files>
  <action>
**i18n labels (both zh and ja wizard.json):**

1. In `zh/wizard.json`, find the `hobbies` label key under `reviewExtraction.fields` (or similar path) and rename to `other`:
   - Key: `"other": "其他信息"` (was `"hobbies": "兴趣爱好"`)

2. In `ja/wizard.json`, do the same:
   - Key: `"other": "その他"` (was `"hobbies": "趣味"`)

3. If there are any i18n keys for `linkedin`, `website`, `gpa`, `notes` under the Japanese resume translation review section, remove them. These fields are no longer displayed.

**Completeness utility (`completeness.ts`):**

4. Find references to `hobbies` in the completeness scoring logic and rename to `other`.
   - This is likely in a field list or property access like `data.hobbies` → `data.other`

5. If completeness scoring references `linkedin`, `website`, `gpa`, or `notes` for JpResumeData scoring, remove those references.
  </action>
  <verify>
- Read both wizard.json files and confirm `other` key exists, `hobbies` key removed
- Read completeness.ts and confirm `other` used, no `hobbies`/`linkedin`/`website`/`gpa`/`notes` references
- Run: `cd frontend && npx tsc --noEmit` to verify no TypeScript errors
  </verify>
  <done>i18n labels updated for hobbies→other in both locales. Completeness utility references updated fields only. No dead field references anywhere in frontend.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors in frontend
2. No grep hits for `hobbies` in frontend/src/ (except possibly in git history)
3. No grep hits for `linkedin|website` in frontend/src/components/review/JpResumeFieldEditor.tsx
4. No grep hits for `\.gpa|\.notes` in frontend/src/
5. i18n keys for `other` exist in both zh and ja locales
</verification>

<success_criteria>
- Chinese resume editor displays "其他信息" / "その他" field instead of hobbies
- Japanese resume editor no longer shows linkedin, website, gpa, notes inputs
- Completeness scoring correctly references all current fields
- TypeScript compilation passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-workflow-data-cleanup/09-02-SUMMARY.md`
</output>
